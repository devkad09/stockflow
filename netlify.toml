[build]
  command = "prisma generate && prisma db push --accept-data-loss && tsx prisma/seed.ts && next build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[build.environment]
  NODE_VERSION = "20"
  NPM_FLAGS = "--legacy-peer-deps"
